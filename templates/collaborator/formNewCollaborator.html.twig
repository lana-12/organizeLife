{% extends 'base.html.twig' %}

{% block title %}Nouveau Projet {% endblock %}


{% block body %}

    <section>
    
        {# {% if editMode %}
            <h2 class="mt-4 center blue fs-1"> Modifier</h2>
        {% else %}
            <h2 class="mt-4 center blue fs-1"> Nouveau</h2>
        {% endif %} #}

    </section>
            <h2 class="mt-4 center blue fs-1"> nouveau coll</h2>

    <section class="bg-secondary text-white p-3 m-3">
    {# {{ dump(app.user) }} #}
    <h3 class="text-center">Administrateur : </h3>
        <p>{{ app.user.firstname }}</p>
        <p>{{ app.user.lastname }}</p>
        <p>{{ app.user.email }}</p>
    </section>

        {# <div class="d-flex ">
            {{ form_row(projectForm.name) }}
        </div>

        <div>
            {{ form_row(projectForm.description) }}
        </div> #}
    
    <main class="container mt-3">
		<div class="row">
			<div class="col ">
			

        {{ form_errors(collaboratorform) }}
        {{ form_start(collaboratorform,  { 'attr': {'class': 'formProfil'} }) }}

			<fieldset class="mb-3">
				<div>
                    {{ form_rest(collaboratorform) }}
                </div>

			

        {{ form_end(collaboratorform) }}
			</div>
		</div>
	</main>


{% endblock %}
