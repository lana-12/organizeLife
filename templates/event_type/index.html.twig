{% extends 'base.html.twig' %}

{% block title %}OrgalizeLife | Type d'Events{% endblock %}

{% block body %}
	<div class="">
		{# {% if eventsType %} #}
			{# Array associatif pour mapper les noms de route aux fichiers Twig  #}
			{% set routeName = app.request.attributes.get('_route') %}
			{% include {
                'event.type.new': './_fromEventType.html.twig',
                'event.type.edit': './_fromEventType.html.twig',
            }[routeName] ?? '' %}

		{# {% else %} #}
			<div class="alert alert-danger alert-dismissible" role="alert">
				<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
				<div class="alert-message">
					<p>Pas d'évènement en cours ...</p>
					<a href="{{ path('admin.index') }}">Espace perso</a>
				</div>
			</div>
		{# {% endif %} #}
	</div>
{% endblock %}